<h1>All doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li><%= doctor.name %>, <%= doctor.email %> <%= link_to "Delete", doctor, 
                                                    method: :delete,
                                                    data: { confirm: "You sure" }%>
    </li>
  <% end %>
</ul>
